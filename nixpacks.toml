[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install torch --index-url https://download.pytorch.org/whl/cpu", "pip install fastapi uvicorn transformers pydantic"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
